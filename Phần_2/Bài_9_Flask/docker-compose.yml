version: '3.8'

services:
  flask:
    build: .
    container_name: flask_app
    ports:
      - "5000:5000"
    volumes:
      - .:/app
    restart: always
    environment:
      FLASK_APP: app.py
      FLASK_ENV: development
